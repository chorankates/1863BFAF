language: ruby

rvm:
  - 2.2.2 # this is what 1863BFAF itself runs on

# need to be able to install packages
sudo: true

# check/report on presence of java, perl, php, python and curl/wget
before_install:
  # looks like we have everything except php
  - sudo apt-get install php5
  - ruby resources/explore_travis.rb

notifications:
  email: false

bundler_args: --without test --jobs 3 --retry 3
# TODO this is temporary, still working it out
#script: bundle exec rake test build
script: bin/1863BFAF; rake build
